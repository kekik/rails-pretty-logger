RSpec.describe Rails::Pretty::Logger do
  it "has a version number" do
    expect(Rails::Pretty::Logger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
